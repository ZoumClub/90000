"use client";

import { useFeatures as useFeaturesList } from "@/lib/modules/features";

// Re-export the hook with any additional application-specific logic if needed
export const useFeatures = useFeaturesList;